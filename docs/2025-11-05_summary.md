# 2025-11-05 작업 요약

- 캘린더 타임라인 뷰에서 가로 스크롤 시 날짜 구분선이 사라지는 문제를 확인하고, 그리드 폭을 날짜 수에 맞춰 고정하도록 `GRID_FIXED_COLS`, `GRID_FIXED_WIDTH` 상수를 도입했습니다.
- 월/일 헤더, 일정 행, 배경 격자가 모두 동일한 고정 폭과 컬럼 정의를 사용하도록 수정해 26일 이후 구간에서도 선이 끊기지 않도록 했습니다.
- 변경 내용은 `components/views/calendar/components/CalendarTimelineView.tsx`에 적용됐으며, 셀 최소 폭은 72px로 설정했습니다.
- 사이드바 없이 `CalendarHeader`를 복원하고, 각 캘린더 항목에 톱니 아이콘을 추가해 설정 모달(`CalendarManageModal`)을 통해 이름·색상 수정과 삭제가 가능하도록 리팩터링했습니다.
- `useResizeObserver`를 활용해 타임라인 셀 폭과 좌측 분류 폭을 화면 너비에 맞춰 동적으로 계산하는 로직을 추가했습니다.
- `CalendarDetailsPanel` UI를 재설계해 일정 타임라인 카드, 액션 버튼, 시간 프리셋이 포함된 폼 등으로 전문성과 사용성을 높였고, 일정 추가 시 목록 대신 작성 화면으로 전환되도록 UX를 정리했습니다.
- 월간 뷰(`CalendarMonthView.tsx`)에서 다일 일정이 첫날뿐 아니라 종료일까지 막대로 이어지도록 이벤트 span 로직을 적용하고, DayEventPill을 variant 기반 디자인으로 개선했습니다.
- 월간 뷰 막대에서 투명도를 제거해 진한 색상으로 표시하고 hover 시 일정 요약 툴팁을 노출합니다. 하루에는 최대 두 개의 막대만 표시되며, 날짜 셀 상단에는 회색 배경·검은 글씨의 `n개 일정` 뱃지가 표시됩니다.
- Agenda 뷰의 자체 스크롤을 제거해 상위 컨테이너에서만 스크롤이 발생하도록 단순화했습니다.
