# 2025-11-06 작업 요약

## 변경 사항
- components/views/issues/KanbanView.tsx를 참고 레퍼런스(@reference/issue) 스타일의 다중 탭 뷰로 재구성하여 생산 일정, 잉크 준비, 외주, 설비 섹션을 한 화면에서 탐색할 수 있게 변경했습니다.
- shadcn/ui 없이 Tailwind 유틸만으로 카드, 표, 배지 등을 구성하고, 레퍼런스 데이터에서 발췌한 목데이터를 컴포넌트 상단에 정리했습니다.
- 요약 칩·상세 섹션·알림 등 공통 UI 헬퍼를 같은 파일에 정의해 추후 스토어/실데이터로 바꾸기 쉽게 구조화했습니다.

## TODO / 다음 시작 지점
1. components/views/issues/KanbanView.tsx:29 TABS 정의와 1~185행에 있는 목데이터 배열을 zustand 또는 API 응답으로 대체할 계획 수립 → 실제 데이터 소스로 교체할 때는 동일한 키(id, status 등)를 유지하세요.
2. components/views/issues/KanbanView.tsx:206 이후 뷰 컴포넌트(ProductionView, JobSheetView, PaintingView, SubcontractView, ResourceView)에 필터/액션을 연결해야 합니다. 각 섹션 상단 버튼에 onClick 핸들러를 채우고, 필요 시 별도 모달 컴포넌트를 분리하세요.
3. 모바일 대응 검증: KanbanView 렌더 후 DevTools에서 width 360~768px 구간을 확인하고, 필요 시 grid 분기(sm:grid-cols-2 등)를 조정합니다. 해당 점검은 상단 nav와 카드 영역 중심으로 진행하면 됩니다.

위 TODO를 실행하기 전 이 파일과 components/views/issues/KanbanView.tsx를 다시 읽고, 단계별로 체크하면서 진행하면 됩니다.

