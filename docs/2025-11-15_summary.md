# 2025-11-15 작업 요약

## 주요 변경 사항
- Note 대시보드 전면 개편
  - `components/docs/DocsDashboard.tsx`를 중심으로 폴더 그리드/문서 리스트·카드 뷰, SortMenu/FilterMenu, CreateFolderModal 등을 도입해 구글 드라이브 유사 UX를 구현.
  - 문서 메타(`lib/docs.ts`)에 `fileSize`를 추가해 리스트 정렬과 표시가 가능하도록 하고, 새 note-drive 하위 컴포넌트(문서/폴더/필터/정렬 UI)를 분리.
- 사이드바 경험 향상
  - `hooks/useSidebarCollapse.ts`로 사이드바 축소 상태를 로컬스토리지에 유지하고, `components/layout/Sidebar.tsx`에 축소형 런처와 고객센터 버튼을 추가.
  - 모바일 Drawer(`components/ui/Drawer.tsx`) 상단에 `components/layout/MobileNavHeader.tsx`를 삽입해 Fourier 로고 + 사용자 아바타 기반 헤더 제공.
  - `app/*/layout.tsx` (chat/docs/issues/worksheet/members)에서 축소 상태를 공유하고 Drawer 헤더를 교체.

## 추가 메모
- `npm run build`로 전체 변경 사항 검증 완료.
- Drawer 헤더와 사이드바 축소 토글은 추후 다른 서브앱에도 동일하게 적용 가능.***
