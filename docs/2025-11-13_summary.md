# 2025-11-13 작업 요약

## 오늘 한 일
- Docs 첫 진입 시 바로 에디터로 진입하던 흐름을 대시보드 중심 UX로 개편했다. 구글 드라이브 스타일 카드/리스트/필터 UI를 만든 `DocsDashboard`를 `/docs` 루트에 배치하고, 실제 문서 편집은 `/docs/[docId]` 라우트에서 열리도록 분리했다. Outline/History 패널과 드로어는 상세 문서 라우트에서만 노출된다.
- 문서 메타/콘텐츠/Snapshot 키를 다루는 헬퍼(`lib/docs.ts`)를 정리하고, 더미 데이터는 `lib/mocks/docs.ts`로 이관했다. Command Palette·Sidebar·DocView가 같은 메타 소스를 바라보면서 마지막 열람 시각, 즐겨찾기, 생성 흐름이 일관적으로 관리된다.
- Issues 모듈에서 사용하던 `mocks/kanban.ts` 또한 `lib/mocks/kanban.ts`로 이동해 모킹 데이터 위치를 통합했다. `@mocks/*` 경로 alias는 제거하고 실제 경로(`@/lib/mocks/*`)를 사용하도록 정리했다.

## 남은 TODO 제안
1. Sidebar/Command Palette의 Docs 리스트도 `readDocCollection` 데이터를 바로 그려서 새 문서가 실시간 반영되도록 연결.
2. Docs 대시보드에 템플릿 단축 버튼이나 최근 활동 로그를 추가해 협업 중인 동료 업데이트를 노출.
3. `lib/docs.ts`의 Broadcast 이벤트를 활용해 다른 탭에서도 문서 즐겨찾기/삭제가 즉시 반영되는지 QA 후 Debounce나 Reconcile 로직 보강.*** End Patch
